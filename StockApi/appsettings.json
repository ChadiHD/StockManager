{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=ApiAuthDb;Trusted_Connection=True;MultipleActiveResultSets=true",
    "SMDatabase": "Data Source=SITIHAPIB;Initial Catalog=SMDatabase;Integrated Security=True;Connect Timeout=60;Encrypt=False;TrustServerCertificate=False;ApplicationIntent=ReadWrite;MultiSubnetFailover=False"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Icecat": {
    "//": "Product-content lookups used to fill in missing product images. Left blank here on purpose — put UserName/ApiKey in user-secrets, not in source control. With no UserName the client reports IsConfigured=false and enrichment simply does nothing.",
    "UserName": "",
    "ApiKey": "",
    "Language": "en",
    "//EnrichmentEnabled": "Lets the background worker fill in missing images. A finished feed sync signals it, so pressing Sync starts a pass immediately; with no Icecat UserName above the worker does nothing regardless. The manual POST api/Product/Catalog/EnrichImages endpoint works either way.",
    "EnrichmentEnabled": true,
    "BatchSize": 50,
    "//BatchPauseSeconds": "Pause between batches while a backlog is draining — short, so a sync's new products are enriched in minutes, but not so short that Icecat is hammered.",
    "BatchPauseSeconds": 2,
    "//IdleHours": "Fallback sweep when no sync has signalled, for products added by any route other than a feed import.",
    "IdleHours": 6
  },
  "Feeds": {
    "//": "The nightly distributor feed sync. Feeds themselves are defined in dbo.DistributorFeed and managed from /admin/feeds; this section only decides whether and when the scheduler runs them.",
    "//SyncEnabled": "Off by default on purpose. A development database usually carries the real feed credentials — that is what makes them worth testing against — so pressing F5 must not open an SFTP session to a distributor. Turn it on per environment.",
    "SyncEnabled": false,
    "//SyncAtUtc": "Time of day, UTC, 24-hour. One value for every store: the right hour is whenever a distributor finishes dropping its file, which the platform cannot infer from a store's country. A host that starts after the hour waits for tomorrow rather than syncing at once, so a restart loop cannot re-import every feed.",
    "SyncAtUtc": "02:00"
  },
  "FeedSecrets": {
    "//": "Distributor feeds are defined in dbo.DistributorFeed and managed from /admin/feeds. No credential is ever stored here — this section only selects WHERE credentials are kept. Override per environment in appsettings.Development.json / appsettings.Production.json.",
    "Provider": "DataProtection",
    "KeyVaultUri": ""
  }
}
