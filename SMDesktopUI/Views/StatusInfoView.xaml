<UserControl x:Class="SMDesktopUI.Views.StatusInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="260" d:DesignWidth="480"
             MinWidth="400">
    <Border Style="{StaticResource Card}" Padding="28">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Background="{StaticResource TealBrush}" CornerRadius="3"/>
                <TextBlock x:Name="Header" Grid.Column="1" Style="{StaticResource PageTitle}"
                           FontSize="22" Margin="14,0,0,0" TextWrapping="Wrap"/>
            </Grid>
            <TextBlock x:Name="Message" Grid.Row="1" Margin="20,20,0,24"
                       TextWrapping="Wrap" MaxWidth="520"
                       AutomationProperties.LiveSetting="Assertive"/>
            <Button x:Name="Close" Grid.Row="2" Content="_Close" IsDefault="True"
                    HorizontalAlignment="Right" MinWidth="100"/>
        </Grid>
    </Border>
</UserControl>
