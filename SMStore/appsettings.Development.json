{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Sites": {
    "//ForceSiteKey": "Pins every request to one store, ignoring the host. Set it to a SiteKey to work on a real store's branding and content from localhost. Development only — Program.cs refuses to start with it set elsewhere."
  }
}
