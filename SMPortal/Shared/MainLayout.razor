@inherits LayoutComponentBase

@* The operations portal has been replaced by the /admin area, which supplies its own shell via
   AdminLayout. What remains here is the chrome for the pages that sit outside that area —
   sign-in and sign-out — plus the fallback layout App.razor uses for routing states. *@
<main class="auth-layout">
    @Body
</main>
